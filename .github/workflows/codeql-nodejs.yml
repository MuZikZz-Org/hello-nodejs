
name: Sonar scanner
on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  CodeQL:
    # needs: Build_Docker_ACR
    name: "CodeQL nodejs"
    uses: MuZikZz-Org/action-workflow/.github/workflows/codeql-nodejs.yaml@main
